name: Bump version using SemVer

on:
  push:
    branches:
      - "master"

jobs:
  build-dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: artdolya/actions/dotnet/build.yml@master
        with:
          work-dir: ./src/Net
          dotnet-version: 8.x

      - uses: artdolya/actions/bump.yml@master
        with:
          checkout: false
