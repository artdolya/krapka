name: Build

on:
  push:
    branches:
      - "*"
      - "!master"

jobs:
  build-dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: artdolya/actions/dotnet/build@master
        with:
          work-dir: ./src/Net
          dotnet-version: 8.x
