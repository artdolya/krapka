name: Build

on:
  push:
    branches:
      - "*"
      - "!master"

jobs:
  build-dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: artdolya/actions/.github/actions/dotnet/build.yml@master
        with:
          work-dir: ./src/Net
          dotnet-version: 8.x
